SEP:show_age(age: number)
if age is EMPTY :age_missing
return "Age unknown"
endif:age_missing
return "Age " + string(age)
end_SEP:show_age

SEP:main
number age = EMPTY
print show_age(age)

age = 30
print show_age(age)

age = EMPTY
print age is EMPTY
end_SEP:main
